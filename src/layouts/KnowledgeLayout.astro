---
import ContentLayout from './ContentLayout.astro';

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<ContentLayout title={title} description={description}><slot /></ContentLayout>